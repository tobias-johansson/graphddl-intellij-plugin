# graphddl-intellij-plugin

Adds basic syntax highlighting for Graph Ddl files

## Development

- Launch an instance of IntelliJ with plugin
  ```
  ./gradlew runIde
  ```
- Build the plugin zip (`build/distributions/`)
  ```
  ./gradlew buildPlugin
  ```
